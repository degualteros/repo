Instrucciones de GIT

git log
git log -oneline

Subir cambios a repositorio remoto

1. Crear usuario en github
2. Crear repositorio en GitHub
3. Vincular el repositorio remoto en el local
	git remote add [nombre alias del remoto] [url]
para listar: 
	git remote -v
4. Empujar cambios al remoto dado de alta en paso 3.
4.1 Si sale error entonces:
	git commit . (para todos los archivos)
	git commit [archivo] (para un archivo puntual)
	git pull [alias] [rama local] --allow-unrelated-histories
	y despues: 
	git push [alias] [rama local]

7. Creacion de ramas:
	git branch (ver las ramas creadas)
	git branch [nombre_rama] (crear rama)
	git checkout [nombre_rama] (cambiar la rama)
	git checkout -b [nombre_rama_X] (crea la rama y la selecciona)
	git branch -d [nombre_rama] (borra la rama seleccionada)
	// video en minuto 17:36 ///


6. Crear repositorio en GitHub y despues trabajar (clonar al local)
ir a directorio donde se quiere clonar:
 	cd [directorio]
 	git clone [url]
 	git remote -v